{
   "Ingredients": [
    <% for i in 0..@Idrecipe.ingredients.length-1 %>
        <%if i== @Idrecipe.ingredients.length-1%>
            "<%= @Idrecipe.ingredients[i].nom.html_safe%>"
        <%else %>
            "<%= @Idrecipe.ingredients[i].nom.html_safe%>",
        <%end%>
    <% end %>
   ]
}